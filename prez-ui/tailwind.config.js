/** @type {import('tailwindcss').Config} */
module.exports = {
    // presets: [preset],
    theme: {
        extend: {
            colors: {
                primary: {
                    DEFAULT: "cornflowerblue",
                },
            },
        },
    },
}